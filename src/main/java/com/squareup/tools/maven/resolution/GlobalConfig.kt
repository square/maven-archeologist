package com.squareup.tools.maven.resolution

/** Some global configuration values, which should be set from command line flags or properties */
object GlobalConfig {
  /** Allows debug and verbose (info) level logging. */
  var debug = false

  /** Allows verbose (info) level logging. */
  var verbose = false
}
